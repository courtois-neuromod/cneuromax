"""Deep Learning component of the CNeuroMax library.

Abbreviations used throughout this module:

Misc:

HPO is short for Hyper-Parameter Optimization

Hydra:

``DictConfig`` is short for ``omegaconf.DictConfig``.

Lightning:

``LightningModule`` is short for ``lightning.pytorch.LightningModule``.

``LightningDataModule`` is short for
``lightning.pytorch.LightningDataModule``.

``WandbLogger`` is short for
``lightning.pytorch.loggers.wandb.WandbLogger``.

``Trainer`` is short for ``lightning.pytorch.Trainer``.

PyTorch:

``nn.Module`` is short for ``torch.nn.Module``.

``Tensor`` is short for ``torch.Tensor``.

``Optimizer`` is short for ``torch.optim.Optimizer``.

``LRScheduler`` is short for ``torch.optim.lr_scheduler.LRScheduler``.

Jaxtyping:

``Float`` is short for ``jaxtyping.Float``.
"""
