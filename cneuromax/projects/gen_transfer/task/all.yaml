# @package _global_
defaults:
  - /space: rl_control_nevo
  - /agent: gen_transfer
  - _self_

hydra:
  launcher:
    tasks_per_node: 40

config:
  agents_per_task: 1
  total_num_gens: 50
  env_transfer: true,false
  mem_transfer: true,false
  fit_transfer: true,false

space:
  config:
    env_name: >
      Acrobot-v1,CartPole-v1,MountainCarContinuous-v0,MountainCar-v0,Pendulum-v1
